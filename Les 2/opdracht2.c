#include <stdio.h>

int main( void ) {
    int diameter;
    printf("Wat is de diameter van de cirkel? ");
    scanf("%d", &diameter);  
    printf("Dit is de diameter, %d ",diameter);
    return 0;
}